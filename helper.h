#ifndef HELPER_H // Checks if HELPER_H is not yet defined. GUARD CLAUSE
#define HELPER_H // If not yet, Define HELPER_H

	// Declare Functions for pt1_1.cpp to implement
	int addition(int x, int y); // require parameters for the function to run
	int subtract(int x, int y); // require parameters for the function to run
	double multiply(double x, double y); // require parameters for the function to run
	double divide(double x, double y); // require parameters for the function to run

#endif // Ends the IF